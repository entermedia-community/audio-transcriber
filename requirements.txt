# ── Core ──────────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9      # file uploads
pydantic>=2.7.0
pydantic-settings>=2.3.0

# ── Transcription ─────────────────────────────────────────────────────────
# faster-whisper: CTranslate2-based Whisper — ~4× faster than openai-whisper
# int8 quantization halves memory use with negligible accuracy loss
faster-whisper>=1.0.3

# ── Speaker diarization ───────────────────────────────────────────────────
# pyannote.audio 3.x — state-of-the-art diarization, free on HuggingFace
pyannote.audio>=3.3.1
torch>=2.2.0
torchaudio>=2.2.0

# ── Optional: GPU acceleration ────────────────────────────────────────────
# Uncomment for CUDA builds (replace cu13 with your CUDA version):
torch>=2.2.0+cu13 --extra-index-url https://download.pytorch.org/whl/cu13

librosa>=0.11.0
soundfile>=0.13.1